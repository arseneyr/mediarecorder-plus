{
  "name": "mediarecorder-plus",
  "version": "0.4.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "clang-format": "^1.4.0",
    "cross-env": "^7.0.2"
  },
  "scripts": {
    "build": "cross-env docker run --rm -it -v %cd%:/src -w /src/encoders trzeci/emscripten make",
    "clean": "cross-env docker run --rm -it -v %cd%:/src -w /src/encoders trzeci/emscripten make clean"
  }
}
